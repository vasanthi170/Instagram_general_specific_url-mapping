from django.shortcuts import render
from django.http import HttpResponse

# Create your views here.
def hearts(request):
    return HttpResponse('This section is responsible for displaying the Notifications...')
